<div>
    <div class="flex flex-row justify-between items-center bg-gray-100 p-4 shadow rounded mb-2">
        <h1 class="font-bold text-gray-700"><?= $title ?></h1>

        <a href="<?= route('products.new') ?>" class="btn-success">
            Cadastrar Produto
        </a>
    </div>

    <table class="min-w-full bg-white rounded-lg shadow">
        <thead>
            <tr class="bg-gray-200 text-gray-700 text-left text-sm uppercase font-semibold">
                <th class="py-2 px-4 border-b text-center">ID</th>
                <th class="py-2 px-4 border-b text-center">Nome</th>
                <th class="py-2 px-4 border-b text-center">Descrição</th>
                <th class="py-2 px-4 border-b text-center">Marca</th>
                <th class="py-2 px-4 border-b text-center">Preço</th>
                <th class="py-2 px-4 border-b text-center">Ações</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($products as $product) : ?>
                <tr class="hover:bg-gray-50">
                    <td class="py-2 border-b text-gray-800 text-center"><a href="<?= route('products.show', ['id' => $product->getId()]) ?>">#<?= $product->getId() ?></a></td>
                    <td class="py-2 border-b text-gray-800 text-center"><?= $product->getName() ?></td>
                    <td class="py-2 border-b text-gray-800 text-center"><?= $product->getDescription() ?></td>
                    <td class="py-2 border-b text-gray-800 text-center"><?= $product->getBrand() ?></td>
                    <td class="py-2 border-b text-gray-800 text-center">R$ <?= number_format($product->getPrice(), 2, ',', '.')  ?></td>
                    <td class="py-2 border-b text-gray-800 text-center">
                        <div class="flex flex-row justify-center items-stretch gap-2">
                            <a href="<?= route('products.edit', ['id' => $product->getId()]) ?>">
                                <button class="btn-info">
                                    Editar
                                </button>
                            </a>

                            <form action="<?= route('products.destroy', ['id' => $product->getId()]) ?>" method="POST" class="m-0">
                                <input type="hidden" name="_method" value="DELETE">
                                <button class="btn-danger" type="submit">
                                    Excluir
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <div class="mt-2">
        <?php $paginator->renderPagesNavigation(); ?>
    </div>
</div>
